extends base

block content
    if errorMessage
        span=errorMessage
    form(method="POST")
        input(name="address", type="text", placeholder="address", required)
        input(type="submit", value="Edit")